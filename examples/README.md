# examples/

Placeholder for demo artifacts. Nothing here yet — everything listed below
is planned output of work described in `docs/DEMO_CANDIDATES.md` and
`docs/CONTAINMENT_DEMO_SCRIPT.md`, and will be added only as real runs
produce it.

Eventually this directory will hold:

- **Sample analysis reports** — `mri analyze` / `mri blast-radius` output
  captured from real runs against the showcase repository (once chosen).
- **Before/after screenshots** — terminal captures showing, e.g., a
  blast-radius query with confirmed vs ambiguous-only dependents kept
  separate, or a containment-demo block landing pre-execution.
- **Reproduction notes** — exact commands and repo commit hashes used, so
  any shown result can be independently re-run.

Nothing in here should be hand-written to look like tool output.
